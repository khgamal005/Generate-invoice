# Use official Node.js image
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy everything
COPY . .

# Expose Next.js dev server port
EXPOSE 3000

# Run in dev mode (can be overridden in docker-compose.override.yml)
CMD ["npm", "run", "dev"]
